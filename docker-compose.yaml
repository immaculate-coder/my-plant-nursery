version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=immaculate
      - MONGO_INITDB_ROOT_PASSWORD=notverysecret
      - MONGO_INITDB_DATABASE=gardenia_dreams